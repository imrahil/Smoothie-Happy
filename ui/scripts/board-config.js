// -----------------------------------------------------------------------------
// board config model
// -----------------------------------------------------------------------------

var ConfigModel = function(parent) {
    // self alias
    var self = this;

    // set parent model
    self.parent = parent;
};

ConfigModel.get = function(key) {

};

ConfigModel.set = function(key, value) {

};
