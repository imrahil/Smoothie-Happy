/**
* {$title} - {$description}.
* @author   {$author}
* @see      {@link {$srcUrl}}
* @build    {$build}
* @date     {$date}
* @version  {$version}
* @license  {$license}
* @namespace
*/
var {$name} = {};

(function (sh) {
    'use strict';

    /**
    * @property {String} - API version.
    * @default
    * @readonly
    */
    sh.version = '{$version}';

})({$name});

{$sh.network.js}
